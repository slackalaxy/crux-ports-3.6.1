# Description: Terminus font
# URL: http://terminus-font.sourceforge.net/
# Maintainer: Petar Petrov, slackalaxy at gmail dot com
# Depends on: xorg-bdftopcf xorg-mkfontscale
# Conflicts: console-font-terminus xorg-font-terminus

name=terminus-font
version=4.49.1
release=1
source=(http://downloads.sourceforge.net/$name/$name-$version.tar.gz)

build() {
	cd $name-$version
	
	chmod +x configure
	./configure --prefix=/usr \
	--x11dir=/usr/share/fonts/X11/terminus \
	--psfdir=/usr/share/kbd/consolefonts
		
	make
	make install install-pcf-8bit install-psf-ref install-psf-vgaw install-otb DESTDIR=$PKG
}
